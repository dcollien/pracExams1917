
// Implement the concat function below
// so that the two input lists (firstList, secondList)
// are concatenated together (joined) and the resulting list
// is returned. firstList comes before secondList after joining.
// 
// This function will not allocate any new list nodes.
//
// Lists may be empty.
//
// You need to pass the tests in testConcat.c
//
// Compile and run the tests using 
// gcc -Wall -Werror -O -o tester concat.c testConcat.c
// ./tester

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include "concat.h"

list concat(list firstList, list secondList) {
   // write your code here
   
   return firstList;
}
